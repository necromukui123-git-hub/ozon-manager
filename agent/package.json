{
  "name": "agent",
  "version": "1.0.0",
  "description": "该 Agent 用于在本机执行自动化任务，支持两种模式：",
  "main": "agent.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.5",
    "dotenv": "^17.2.4",
    "playwright": "^1.58.2"
  }
}
